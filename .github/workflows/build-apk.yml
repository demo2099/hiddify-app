name: Build APK with ECH Support
on:
  push:
    branches: [ech-support]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo Z2l0QGdpdGh1Yi5jb206
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false
          ref: ech-support
      - run: echo done
